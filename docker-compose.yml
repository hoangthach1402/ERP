version: '3.8'

services:
  manufacturing-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: manufacturing-erp
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - DB_PATH=/app/database/manufacturing.db
      - JWT_SECRET=${JWT_SECRET:-change_me_in_production}
      - SESSION_SECRET=${SESSION_SECRET:-change_me_in_production}
      - TELEGRAM_RAP_BOT_TOKEN=${TELEGRAM_RAP_BOT_TOKEN:-8278317654:AAFlydIUH0qRBSz30gVqLgV6NkAakjeeDBE}
      - TELEGRAM_RAP_GROUP_ID=${TELEGRAM_RAP_GROUP_ID:--1003684379890}
      - TELEGRAM_RAP_ENABLED=${TELEGRAM_RAP_ENABLED:-true}
      - TELEGRAM_CAT_BOT_TOKEN=${TELEGRAM_CAT_BOT_TOKEN:-8471736527:AAGMusSjEVB97VmhpsBxra4A7FMiX1D9t6k}
      - TELEGRAM_CAT_GROUP_ID=${TELEGRAM_CAT_GROUP_ID:--1003511311254}
      - TELEGRAM_CAT_ENABLED=${TELEGRAM_CAT_ENABLED:-true}
    volumes:
      - ./src:/app/src
      - ./views:/app/views
      - ./public:/app/public
      - ./database:/app/database
      - ./logs:/app/logs
      - /app/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/product/dashboard"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - manufacturing-network
    stdin_open: true
    tty: true

  # Optional: Nginx reverse proxy for production
  nginx:
    image: nginx:alpine
    container_name: manufacturing-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - manufacturing-app
    restart: unless-stopped
    networks:
      - manufacturing-network

networks:
  manufacturing-network:
    driver: bridge
